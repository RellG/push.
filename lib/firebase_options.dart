// Placeholder until `flutterfire configure` generates the real file.
// Run: dart pub global activate flutterfire_cli && flutterfire configure
// That command overwrites this file with the project's actual options.
import 'package:firebase_core/firebase_core.dart';

class DefaultFirebaseOptions {
  static FirebaseOptions get currentPlatform {
    throw UnsupportedError(
      'firebase_options.dart has not been generated yet. '
      'Run `flutterfire configure` to connect the Firebase project.',
    );
  }
}
